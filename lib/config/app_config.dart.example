// ─────────────────────────────────────────────────────────────────────────────
// Copy this file to app_config.dart (git-ignored) and fill in your URL.
// In CI set the WEB_APP_URL GitHub secret — the workflow creates this file
// automatically from that secret value.
//
// Get the URL from:
//   Google Sheet → Extensions → Apps Script → Deploy → New deployment → Web App
// ─────────────────────────────────────────────────────────────────────────────

const String kWebAppUrl = 'PASTE_YOUR_WEB_APP_URL_HERE';

